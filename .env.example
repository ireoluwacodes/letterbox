# Convex deployment URL (React client — use the *.convex.cloud URL from the dashboard)
VITE_CONVEX_URL=https://tame-ibex-692.convex.cloud

# Optional: public site URL (no trailing slash) for absolute OG/Twitter images and canonical link
# VITE_SITE_URL=https://your-production-domain.com
